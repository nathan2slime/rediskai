/**
 * Empty state for when no connection is active.
 * @example
 * <ActiveConnectionEmpty />
 */
export const ActiveConnectionEmpty = () => {
  return <p className="text-sm text-muted-foreground">No active connection selected.</p>
}
