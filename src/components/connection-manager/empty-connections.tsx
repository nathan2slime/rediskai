/**
 * Empty state content for saved connections.
 * @example
 * <EmptyConnections />
 */
export const EmptyConnections = () => {
  return <p className="text-sm text-muted-foreground">No connections saved yet.</p>
}
